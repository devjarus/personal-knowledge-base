node_modules
.next
out
dist
.env
.env.local
.DS_Store
*.log
.turbo
tsconfig.tsbuildinfo

# Coding-agent pipeline workspace (local only)
.coding-agent/

# KB content — you probably want to track this separately (its own git repo,
# iCloud/Dropbox folder, or S3 sync). Uncomment if you do NOT want it in this repo.
# kb/
